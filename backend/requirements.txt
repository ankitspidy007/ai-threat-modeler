# Aegis Threat backend dependencies (Python 3.10+).

# Web framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.34.0,<1.0.0
python-multipart>=0.0.9,<1.0.0

# Core analysis and modeling
networkx>=3.4.0,<4.0.0
pydantic>=2.6.0,<3.0.0
numpy>=1.26.0,<3.0.0
pyyaml>=6.0.1,<7.0.0
pypdf>=5.5.0,<6.0.0
python-docx>=1.1.2,<2.0.0

# NLP and local model runtime
blingfire>=0.1.8,<1.0.0
transformers>=4.49.0,<5.0.0
torch>=2.2.0,<3.0.0

# Semantic retrieval and local ML
sentence-transformers>=3.4.0,<4.0.0
faiss-cpu>=1.10.0,<2.0.0
scikit-learn>=1.4.0,<2.0.0

# Optional LLM integrations
openai>=1.0.0,<2.0.0
anthropic>=0.18.0,<1.0.0
requests>=2.31.0,<3.0.0

# Testing.
# These are installed in the runtime image too, which is more than it needs;
# splitting them into requirements-dev.txt is tracked separately.
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
pytest-cov>=4.1.0,<5.0.0
# Most of the suite's wall time is engine import, not assertions, so the fast
# lane parallelizes across processes.
pytest-xdist>=3.6.0,<4.0.0
httpx>=0.26.0,<1.0.0
